# Changesets

Run `bun run changeset` in feature branches to describe package changes and choose a semver bump.

When changes land on `main`, the release workflow opens or updates a version PR. Merging that version PR publishes the package to npm.
